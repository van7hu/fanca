--- For something which is not well-structured in thinking, ---

Of course, code for components are all there, we need engine for each component, so they could communicate.

Each engine is a multiprocessing.Process


**** dynamorio

++ Add: C:\fanca\dependencies\DynamoRIO\bin32\client10.c for feedback fuzzer

in Visual studio developer command prompt (I am using 12)
cd C:\fanca\dependencies\DynamoRIO\bin32
compile.bat

